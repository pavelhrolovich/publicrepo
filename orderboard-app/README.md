# Order Board App

Spring Boot REST API for the Silver Bars live order board exercise.

## Stack

- Java 17+
- Spring Boot 4
- Maven

## Run

```bash
mvn -B clean test
mvn spring-boot:run
```

The service starts on port `55518` by default.
